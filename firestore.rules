rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read access to everyone (public data)
      allow read: if true;
      // Deny write access to client-side users (admin SDK bypasses this)
      allow write: if false;
    }
  }
}
